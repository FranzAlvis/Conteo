# Database Connection (PostgreSQL Docker Container - ver docker-compose.yml)
DATABASE_URL="postgresql://conteo_user:conteo_password@localhost:5433/conteo_votos_db?schema=public"

# JWT Authentication
JWT_SECRET="super-secret-key-vicerrectorado-2026"
JWT_EXPIRES_IN="24h"

# Application
PORT=3000
CORS_ORIGIN="http://localhost:5173"

# Carga de archivos (actas de mesa)
UPLOADS_DIR="uploads"
MAX_UPLOAD_SIZE_MB=5
